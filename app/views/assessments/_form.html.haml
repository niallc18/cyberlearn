-# frozen_string_literal: true
= simple_form_for([@course, @assessment]) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :title
    = f.label :questions
    = f.rich_text_area :questions
    = f.label :answers
    = f.rich_text_area :answers
  %p
  .form-actions
    = f.button :submit
